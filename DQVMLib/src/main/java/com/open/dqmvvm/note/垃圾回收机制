垃圾回收机制
    判断对象存活
        引用计数法(废弃）
            引用则计数+1
            引用失效计数-1
            计数为0回收

        可达性分析算法
            遍历GC Root
            可达留存
            不可达清除

    垃圾收集
        复制算法
            内存对象分新生,老生
            将内存分成3块
            新生对象至于内存1区（Eden）
            经过1次GC对象进入内存2区（S0）
            经过15次GC对象进入内存3区（S1）
            大对象直接进入内存3区（S1）
        GC
            标记清理（可达算法）直接标记进行清理
            标记整理（可达算法）标记后整理内存一起清除




